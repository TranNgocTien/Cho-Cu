import 'package:flutter/material.dart';

ValueNotifier<double> marketListen = ValueNotifier(0);
ValueNotifier<double> jobListen = ValueNotifier(0);
ValueNotifier<double> notiListen = ValueNotifier(0);
